version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - env | grep -e NEXT_PUBLIC_SUPABASE_URL -e NEXT_PUBLIC_SUPABASE_ANON_KEY -e SUPABASE_SERVICE_ROLE_KEY -e NEXT_PUBLIC_RAZORPAY_KEY_ID -e RAZORPAY_KEY_ID -e RAZORPAY_KEY_SECRET -e RESEND_API_KEY -e RESEND_FROM_EMAIL -e CONTACT_TO_EMAIL -e ADMIN_USERNAME -e ADMIN_PASSWORD -e ADMIN_SESSION_SECRET -e TELEGRAM_BOT_TOKEN -e TELEGRAM_ADMIN_CHAT_ID -e NEXT_PUBLIC_SITE_URL >> .env.production
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
